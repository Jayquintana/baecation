class Bookings {
  constructor(bookingData, roomData) {
    this.bookingData = bookingData;
    this.roomData = roomData;
  }
  getBookingRoom(roomNumber) {

    
    // should take in a booking room number 
    // compare the room number from the booking data and room data 
    //match the booking to the right room 
    // return the single room object
  }

  getBookingDate() {
    //takes in an argument of a booking id
    //return the booking date that matches the id
    // 
  }
}

module.exports = Bookings ;